//Alterando estilos
//classList
const element = document.querySelector('body')

element.classList.add('active')     //Adicionando classe active no body

element.classList.remove('active')      //removendo classe active do body

element.classList.toggle('active')      //Caso não tenha ele adiciona ao body, caso tenha ele remove do body